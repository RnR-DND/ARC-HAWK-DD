# Continue Here — ARC-HAWK-DD

**Updated:** 2026-04-06
**Branch:** cc

---

## What's Done (Uncommitted — all changes on branch `cc`)

### Session 1 Bug Fixes (8 files, from previous session)
1. **PII Summary endpoint** — `GET /api/v1/scans/:id/pii-summary`
2. **Actions always visible** — Removed hover-only opacity from FindingsTable
3. **Confidence always 45%** — Boosted MLConfidence minimum in sdk_adapter.go
4. **Delete scan button** — `DELETE /api/v1/scans/:id` with cascading delete
5. **Dark theme remnants** — Fixed scans pages (bg-card → bg-white, etc.)

### Session 2 Fixes (this session)
6. **Scanner `username` → `user` field mapping** — `scanner_api.py` normalizer now remaps `username→user` for DB types, `bucket→bucket_name` for storage types, strips platform-only fields
7. **Scanner diagnostic reporting** — Parses hawk_scanner stdout for tables/rows scanned, builds human-readable status message, stores in scan metadata
8. **Backend diagnostic storage** — `CompleteScan` handler stores diagnostics in metadata BEFORE status transition (avoids immutability guard)
9. **Frontend diagnostic display** — Scan detail page shows status_message and warning when 0 tables found
10. **Delete connection route missing** — Added `DELETE /connections/:id` to connections module.go (handler existed but route was never registered)
11. **Delete source from Run Scan modal** — Added trash icon on source cards in ScanConfigModal.tsx with confirmation dialog
12. **FindingDetailDrawer dark theme fix** — Changed `bg-card`→`bg-white`, `border-border`→`border-slate-200`, `bg-secondary`→`bg-slate-50`

### Modified Files (uncommitted, both sessions combined)
- `apps/scanner/scanner_api.py` (normalizer, diagnostics, debug logging)
- `apps/backend/modules/scanning/api/scan_status_handler.go` (diagnostics storage, log import)
- `apps/backend/modules/scanning/module.go` (new routes)
- `apps/backend/modules/scanning/service/sdk_adapter.go` (confidence boost)
- `apps/backend/modules/connections/module.go` (DELETE route added)
- `apps/backend/modules/shared/infrastructure/persistence/scan_run_repository.go` (DeleteScanRun, GetScanPIISummary)
- `apps/frontend/app/scans/[id]/page.tsx` (PII summary, diagnostics display, light theme)
- `apps/frontend/app/scans/page.tsx` (delete button, light theme)
- `apps/frontend/components/FindingsTable.tsx` (actions always visible)
- `apps/frontend/components/findings/FindingDetailDrawer.tsx` (dark theme → light theme fix)
- `apps/frontend/components/scans/ScanConfigModal.tsx` (delete source icon, Trash2 import)
- `apps/frontend/services/scans.api.ts` (deleteScan, getScanPIISummary)

---

## REMAINING: Multi-Source Scanner Bugs (10 bugs from plan)

**Plan file:** `C:\Users\AnshGupta\Downloads\multi_source_scanning_fix_plan.md`

### BUG-1: MySQL Driver Not Registered (Go Backend)
- **File:** `apps/backend/modules/connections/service/connection_test_service.go`
- **Fix:** Add `_ "github.com/go-sql-driver/mysql"` import

### BUG-2: MongoDB missing `args` parameter — CRASH
- **File:** `apps/scanner/hawk_scanner/commands/mongodb.py` line 106
- **Fix:** Add `args` as first argument to `connect_mongodb()` call

### BUG-3: MongoDB reads `username` after normalizer renames to `user`
- **File:** `apps/scanner/hawk_scanner/commands/mongodb.py` lines 87-88
- **Fix:** Change `config.get('username')` → `config.get('user')`, rename param

### BUG-4: CouchDB not in normalizer DB_TYPES + reads `username`
- **File:** `scanner_api.py` normalizer + `couchdb.py`
- **Fix:** Add `'couchdb'` to DB_TYPES, change `username→user` in couchdb.py

### BUG-5: MySQL port string→int crash
- **File:** `apps/scanner/hawk_scanner/commands/mysql.py` line 89
- **Fix:** `port = int(config.get('port', 3306))`

### BUG-6: MongoDB port string→int crash
- **File:** `apps/scanner/hawk_scanner/commands/mongodb.py` line 86
- **Fix:** `port = int(config.get('port', 27017))`

### BUG-7: Redis port string→int crash
- **File:** `apps/scanner/hawk_scanner/commands/redis.py` line 59
- **Fix:** `port = int(config.get('port', 6379))`

### BUG-8: CouchDB port string→int crash
- **File:** `apps/scanner/hawk_scanner/commands/couchdb.py` line 63
- **Fix:** `port = int(config.get('port', 5984))`

### BUG-9: PostgreSQL port coercion (defense-in-depth)
- **File:** `apps/scanner/hawk_scanner/commands/postgresql.py` line 157
- **Fix:** `port = int(config.get('port', 5432))`

### BUG-10: Normalizer should coerce port→int at boundary
- **File:** `scanner_api.py` normalizer
- **Fix:** Add port int coercion after field renaming, add `'couchdb','redis'` to DB_TYPES

---

## Test Infrastructure

Local test databases running via `C:\Users\AnshGupta\Desktop\arc_hawk_infra`:
- **PostgreSQL:** host.docker.internal:5433, db=pii_test_db, user=postgres, pass=password123
- **MySQL:** host.docker.internal:3306, db=pii_test_db, user=archawk, pass=password123
- **MongoDB:** host.docker.internal:27017, db=archawk_vault, user=admin, pass=password123

Each has ~500 rows of PII test data (11 PII types).

---

## How to Resume

```bash
claude
# Then say:
# "Read .continue-here.md and resume — fix the 10 multi-source scanner bugs"
```

After fixing bugs:
1. Rebuild: `docker compose build backend scanner`
2. Restart: `docker compose up -d backend scanner`
3. Test: Add all 3 local DBs as sources, trigger scan, verify findings from all sources
4. Run /qa to verify
5. Commit all changes
