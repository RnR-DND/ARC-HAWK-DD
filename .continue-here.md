# Continue Here — ARC-HAWK-DD

**Updated:** 2026-04-09
**Branch:** cc

---

## Session 6 Changes (THIS SESSION — DPDPA Gap-Close Phase 2)

### P3-0: Ingestion Contract Refactor (Complete)
- `HawkeyeScanInput` refactored from 8 named source fields → `Sources map[string][]HawkeyeFinding`
- Custom `UnmarshalJSON` captures any JSON key (other than `scan_id`) into Sources map
- Backward-compatible: existing scanner JSON format (`"fs": [...], "postgresql": [...]`) works unchanged
- New connectors need zero struct changes
- Fixed `ingestion_handler.go` to use `len(input.Sources) == 0` check
- Backend compiles clean: `go build ./...` passes

### P3: New Connectors (Complete — 13 new connectors)
All in `apps/scanner/hawk_scanner/commands/`:
- `mssql.py` — pymssql + pyodbc fallback
- `sqlite.py` — stdlib sqlite3
- `azure_blob.py` — azure-storage-blob
- `bigquery.py` — google-cloud-bigquery
- `pptx.py` — python-pptx (slides, tables, speaker notes)
- `html_files.py` — beautifulsoup4 (visible text extraction)
- `email_files.py` — stdlib email + extract-msg (EML + MSG)
- `parquet.py` — pyarrow (also handles ORC in same file)
- `avro.py` — fastavro
- `snowflake.py` — snowflake-connector-python
- `redshift.py` — psycopg2 with Redshift endpoint
- `salesforce.py` — simple-salesforce (SOQL on standard + custom objects)
- `kafka.py` — confluent-kafka (time-bounded snapshot scan, 5 min window)
- `kinesis.py` — boto3 (shard iteration, TRIM_HORIZON)
- `jira.py` — jira SDK (issues + comments)
- `hubspot.py` — hubspot-api-client (Contacts, Companies, Deals)
- `ms_teams.py` — Microsoft Graph API via requests (channel messages + chats, app-only auth)
- All wired into `all.py` SUPPORTED_COMMANDS list
- `requirements.txt` updated with all new dependencies

### P3-streaming: Temporal Streaming Supervisor (Complete)
- `apps/backend/modules/scanning/workflows/streaming_supervisor_workflow.go` (NEW)
- Long-running Temporal workflow for Kafka/Kinesis
- Runs micro-batch windows (default 60s), emits to standard ingest endpoint
- ContinueAsNew every 100 windows to prevent history bloat
- Checkpoints Kafka offset + Kinesis sequence number every 5 windows
- Preserves batch ingestion contract: ScanStatusRunning → ScanStatusCompleted per micro-batch

### P4: Data Profiling (Complete)
- `apps/scanner/sdk/sampling.py` — Reservoir sampling (Algorithm R). For tables > 1M rows: 100k sample
- `apps/scanner/sdk/field_profiler.py` — Per-column stats: null_rate, uniqueness_ratio, entropy_score, value_distribution (top-10 anonymized), numeric min/max/mean/stddev
- `hawk_scanner/commands/postgresql.py` — Wired reservoir sampling + field profiling. Findings now include `profiling` dict. Pattern to apply to other DB connectors.

### Benchmark Fixtures (Complete — all 13 categories)
All categories now have true_positives.jsonl + true_negatives.jsonl + edge_cases.jsonl:
- aadhaar (3 files), pan (3 files, was missing edge_cases), gstin (3), ifsc (3), upi (3)
- phone (3), email (3), passport (3), voter_id (3), dob (3)
- name (3), health_record (3), financial_id (3)

---

## What's Done (all sessions, uncommitted)

### DPDPA P0 — Critical Security Fixes
- **P0-1**: PII sample encryption with AES-256-GCM (EncryptString/DecryptString in EncryptionService)
- **P0-2**: N+1 query in compliance_service.go replaced with single JOIN aggregation
- **P0-3**: Redis-backed active_scans in scanner_api.py

### DPDPA P1 — Core Compliance
- **P1-1**: DPDPA 4-factor risk score formula in risk_scoring_service.go
- **P1-2**: dpdpa_obligation_service.go — Sec 4/5/6/8/9/10/17 obligation mapping
- **P1-3**: report_service.go — HTML compliance gap report (print-to-PDF)
- New API endpoints: GET /compliance/dpdpa/gaps and GET /compliance/dpdpa/report

### DPDPA P2 — Classification Enhancement
- **P2-1**: Gemini stub removed from scan_workflows.go
- **P2-2**: Presidio confirmed as Layer 2
- **P2-3**: apps/scanner/sdk/llm_classifier.py — Claude API integration (batch 20, Redis cache 7d, budget 500/scan)
- **P2-4**: Benchmark suite runner at apps/scanner/tests/benchmark_runner.py

### DPDPA P3 — Connectors + P4 — Profiling (THIS SESSION)
- All connector files listed above

### Earlier Sessions
- All items from Session 5 (GST, custom patterns, NER toggle, retention policies)
- Earlier sessions (auth, scanning, connections, remediation, etc.)

---

## What Remains (P5–P7)

### P5 — Security Hardening
- `SanitizeForLog(s string) string` — audit logs for PII leakage
- API key authentication alongside JWT
- Vault/KMS integration docs for encryption key management

### P6 — UI Gaps
- PII density heatmap on `app/discovery/page.tsx`
- DPDPA obligation checklist on `app/compliance/page.tsx` (wire to /compliance/dpdpa/gaps endpoint)
- Chart date range / source / sensitivity filters

### P7 — Infrastructure
- Helm chart at `helm/arc-hawk-dd/`
- Remediation PDF/XLSX export endpoint

---

## How to Resume

```bash
# Check build still passes
cd apps/backend && go build ./...

# Run benchmark suite
cd apps/scanner && python tests/benchmark_runner.py --verbose

# Apply profiling to remaining DB connectors (follow postgresql.py pattern):
# mysql.py, mongodb.py, mssql.py, sqlite.py, snowflake.py, redshift.py, bigquery.py

# Continue with P5 → P6 → P7
```
