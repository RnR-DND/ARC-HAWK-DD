# Continue Here — ARC-HAWK-DD

**Updated:** 2026-04-22
**Active branch:** `ship/audit-fixes-and-cleanup`
**PR:** https://github.com/RnR-DND/ARC-HAWK-DD/pull/30 (open, not yet merged)

---

## User Actions Required BEFORE Merging PR #30

- [ ] **Rotate Supermemory API key** at https://app.supermemory.ai — key `sm_XXDSFvG3…` exposed in chat logs
- [ ] **Generate `SCANNER_SERVICE_TOKEN`** — `openssl rand -hex 32` — set in backend `.env` and scanner env
- [ ] **Neo4j TLS cert** — set `NEO4J_URI=bolt+ssc://neo4j:7687` in docker-compose, configure cert
- [ ] **Temporal TLS** — set `TEMPORAL_TLS_ENABLED=true` in docker-compose, configure cert

---

## Immediate Next Actions (Next Session)

### 1. WS3 — fplearning module (user selected "implement minimally")
- Migration `000045_fp_corrections.up.sql` + `.down.sql`
- `fp_corrections(id, tenant_id, finding_id, pattern_id, correction_type, reason, created_by, created_at)`
- Routes: `POST/GET /fplearning/corrections`, `GET /fplearning/patterns/:id/stats`, `DELETE /fplearning/corrections/:id`
- Service + repo pattern from existing modules (see `modules/scanning/` for pattern)
- Tests: happy path + tenant isolation
- Wire in `cmd/server/main.go` module bootstrap

### 2. WS4 — OpenAPI via swaggo
```bash
cd apps/backend
go get github.com/swaggo/swag/cmd/swag@latest
go get github.com/swaggo/gin-swagger
go get github.com/swaggo/files
swag init -g cmd/server/main.go -o ../../docs/openapi --parseDependency
```
- Add `@Summary`, `@Tags`, `@Security`, `@Success`, `@Failure` to every handler in `modules/*/api/*.go`
- Wire `/swagger/*any` in `cmd/server/main.go` (open in dev, admin-gated in release)
- Add `make openapi` to root Makefile
- Add CI step to `.github/workflows/ci.yml`

### 3. WS5 — Remaining new docs
- `docs/INTEGRATION_GUIDE.md` — full integration walkthrough
- `docs/WEBHOOKS.md` — event catalog + HMAC signing
- `docs/RUNBOOK_E2E.md` — unified "docker-compose up → scan → verify"
- `docs/SCANNER_REFERENCE.md` — Go scanner disambiguation
- `docs/releases/v3.0.0.md` — release notes
- `tests/e2e/full-scan.sh` + `tests/e2e/README.md`

### 4. WS6 — Remaining doc sweeps
- `readme.md` — fix broken links to TODO.md/AGENTS.md (now exist), remove Python scanner refs, add OpenAPI link
- `docs/architecture/INTEGRATION.md` — fix "Scanner Flask :5000" → "Go scanner :8001"
- `docs/architecture/ARCHITECTURE.md` + `overview.md` — bump v1.0.0/v2.1.0 → v3.0.0
- `DEPLOYMENT_RUNBOOK.md` — bump v2.2.0 header, strip `/Users/prathameshyadav/` paths
- `docs/SEAMLESS_SCANNING.md` + `docs/phase1_deployment.md` — mark deprecated, redirect
- `docs/INDEX.md` — rebuild with current file tree
- `CHANGELOG.md` — append ~30 commits since 2026-04-09

### 5. WS7 — Test stubs
- `apps/backend/modules/scanning/api/patterns_handler_test.go` — implement 6 TODO stubs
- `apps/backend/modules/scanning/activities/scan_activities.go` — implement/defer 4 policy TODOs

### 6. Merge PR #30
```bash
gh pr merge 30 --merge
git checkout main && git pull
```

---

## What Was Done This Session (Session 19 — Integration-Readiness Push)

### WS1 — Repo cleanup (commit a25fb77)
- Untracked `apps/backend/.env.production` from git
- Deleted 93 MB scanner binary, graphify-out/, root node_modules/, apps/scanner/.pytest_cache/
- Moved `architecture/*-sop.md` → `docs/architecture/sops/`
- Merged root `WORKFLOW.md` → `CONTRIBUTING.md`; deleted root WORKFLOW.md
- Renamed `lineage_handler_v2.go` → `lineage_handler.go`; dropped V2 from struct/constructor
- Rewrote Helm scanner section: Python/Flask → Go sidecar, port 8000 → 8001, correct env vars
- Deleted Celery helm templates (celery-workers.yaml, celery-keda.yaml) + celeryWorkers values
- Updated helm servicemonitor.yaml — added frontend + postgres ServiceMonitors
- Updated helm alerts.yaml — removed celery group, added api + classification alert groups
- Deleted flat `k8s/` and `infra/k8s/` trees (content merged into helm)
- Added `.agent/agentsys/` to .gitignore

### WS2 — Code fixes (commit d00ee47)
- Split `/health` → `/livez` (always 200) + `/readyz` (503 on dep failure)
- Helm backend probes updated to /livez and /readyz
- Removed `password123` default from NEO4J_PASSWORD in neo4j.go + neo4j_migrate/main.go

### WS5 — New docs (partial)
- `TODO.md` — created (fills broken link; tracks all P2 items)
- `AGENTS.md` — created (fills broken link; documents Claude Code, skills, Ralph, Hive, CI)
- `INTEGRATION_READY.md` — created (consumer checklist for main-platform integration)

### WS6 — Doc sweeps (partial)
- `docs/API.md` — replaced "Authentication not implemented" with current auth scheme description
- `SECURITY.md` — updated supported versions: 3.0.x supported, 2.x end-of-life

---

## Session History
- Sessions 1–15: Core feature build (scan pipeline, findings, remediation, compliance)
- Session 16: Settings route split + full rebuild
- Session 17: Finding-evidence bug fix (MatchedValue field end-to-end)
- Session 18: System audit P0+P1 — all shipped, PR #30 opened
- Session 19: Integration-readiness push — WS1+WS2 complete, WS5+WS6 partial
