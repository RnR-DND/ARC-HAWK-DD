# Continue Here — ARC-HAWK-DD

**Updated:** 2026-04-22
**Active branch:** `ship/audit-fixes-and-cleanup`
**PR:** https://github.com/RnR-DND/ARC-HAWK-DD/pull/30 (open, not yet merged)

---

## Immediate Next Actions

### 1. User actions required BEFORE merging PR #30
- [ ] **Rotate Supermemory API key** at https://app.supermemory.ai — key `sm_XXDSFvG3…` at `.env:69` was exposed in chat logs
- [ ] **Generate `SCANNER_SERVICE_TOKEN`** — `openssl rand -hex 32` — set in backend `.env` and scanner `.env`
- [ ] **Neo4j TLS cert** — set `NEO4J_URI=bolt+ssc://neo4j:7687` in docker-compose, configure cert (P1-5)
- [ ] **Temporal TLS** — set `TEMPORAL_TLS_ENABLED=true` in docker-compose, configure cert (P1-6)

### 2. Merge PR #30
```bash
gh pr merge 30 --merge
git checkout main && git pull
```

### 3. Apply migration 000044
Runs automatically on `docker-compose up`. Verify after next scan:
```bash
psql -c "SELECT id, risk_score FROM findings ORDER BY risk_score DESC LIMIT 5;"
# Should show non-zero risk_score values
```

---

## What Was Done This Session (2026-04-22)

Full `/ship` workflow for the System Audit plan (`main-autoplan-system-audit-20260421-142311.md`).

### 6 commits on `ship/audit-fixes-and-cleanup`
| SHA | Description |
|-----|-------------|
| `b4b1b56` | feat(mobile-nav): hamburger + Sheet drawer for sub-md viewports |
| `9cc44fd` | fix: remove all mock/hardcoded data; wire real API throughout |
| `1681fd4` | fix(audit): resolve all P0 + most P1/P2 findings from system audit |
| `a1f2d85` | test: 16 new test files, 48 tests covering new security paths |
| `a77f1ac` | feat(p1): per-source streaming ingestion + per-finding risk scores |
| `f808cad` | feat(p1): Neo4j + Temporal TLS enforcement in release mode |

### P0 items — all done
- P0-2: Scanner auth (`ServiceTokenAuth` + `ScannerCallbackAuth`; port 8001 internal-only)
- P0-3: Remediation gated behind `REMEDIATION_ENABLED`; 503 banner in UI
- P0-4: Audit trail — `SCAN_STARTED/COMPLETED/FAILED/PARTIAL` events
- P0-5: Ingest chunk retry (2s/4s/8s) + `partial` terminal scan state
- P0-6: Per-scan timeout via `resolveScanTimeout()` + `context.WithTimeout`
- P0-7: Per-tenant semaphore (`tenantScanLimiter`), 429 on overflow
- P0-1: **PENDING USER ACTION** — rotate Supermemory key

### P1 items — all done
- P1-1: Per-source streaming ingestion (`OnBatch` in orchestrator; peak RSS bounded)
- P1-3: DB connection pools on all Go scanner connectors
- P1-5: Neo4j TLS — `bolt://` rejected at startup in release mode
- P1-6: Temporal gRPC TLS — `TEMPORAL_TLS_ENABLED=true` required in release mode
- P1-9: Per-finding risk score via `scoring.ComputeRiskScore`; migration 000044
- P1-11: Per-tenant rate limiting (JWT tenant+user key)
- P1-12: Frontend 401 interceptor with debounce + redirect
- P1-16: Salesforce SOQL allowlist (`validSOQLIdent`)
- P1-2/4/7/8/10/13/15: Verified already implemented in prior sessions

### 16 new test files (48 tests)
Backend: scanner_token, remediation_disabled, scanner_callback_auth, tenant_scan_limiter,
scan_partial, health_handler, rate_limiter.
Scanner: auth_middleware, scan_timeout, db config, salesforce SOQL, ingest retry.
Frontend: ApiClient401, MobileSidebar, RemediationDisabledBanner.

---

## P2 Items — Deferred (Week 2+ per original plan)

| Item | What | Effort |
|------|------|--------|
| P2-2 | Prometheus metrics (goroutines, semaphore depth, vault, ingest failures) | 1 day |
| P2-3 | Audit chain integrity cron + `GET /audit/verify` endpoint | 1 day |
| P2-4 | Connector testcontainer integration tests (Postgres, MySQL, MongoDB, S3) | 2d/connector |
| P2-5 | Retry + circuit breaker for every connector `Connect()` / `StreamFields()` | 1 day |
| P2-6 | Adaptive ingest chunk delay (backpressure from backend response times) | 2h |
| P2-7 | Oracle connector — implement or remove stub | 30min–1day |
| P2-8 | Audit page pagination (hardcoded `limit:200` at `app/audit/page.tsx:53`) | 3h |
| P2-9 | Error toasts on all mutations (~50% currently silent `console.error`) | 4h |
| P2-11 | Auth gate on Prometheus (9090), Grafana (3002), Temporal UI (8088) | 3h |
| P2-12 | Per-tenant Neo4j `tenant_id` on every Cypher node + query filter | 2 days |
| P2-13 | JWT → httpOnly + Secure + SameSite cookies; CSRF tokens; 15min TTL + refresh | 2 days |

---

## Key Files Changed This PR

```
apps/goScanner/api/auth_middleware.go
apps/goScanner/api/handler.go                        — OnBatch streaming + timeout
apps/goScanner/internal/orchestrator/ingest.go       — IngestFindingsBatch + SendScanComplete
apps/goScanner/internal/orchestrator/orchestrator.go — OnBatch in ScanConfig
apps/backend/modules/scanning/api/scanner_callback_auth.go
apps/backend/modules/scanning/api/tenant_scan_limiter.go
apps/backend/modules/scanning/api/scan_status_handler.go
apps/backend/modules/scanning/api/scan_trigger_handler.go
apps/backend/modules/auth/middleware/auth_middleware.go
apps/backend/modules/remediation/service/remediation_service.go
apps/backend/modules/shared/scoring/risk_scorer.go
apps/backend/modules/scanning/service/sdk_adapter.go
apps/backend/modules/shared/domain/entity/finding.go  — RiskScore field added
apps/backend/modules/shared/infrastructure/persistence/finding_repository.go
apps/backend/modules/shared/middleware/rate_limiter.go
apps/backend/modules/shared/api/health_handler.go
apps/backend/modules/scanning/worker/temporal_worker.go
apps/backend/cmd/server/main.go
apps/backend/migrations_versioned/000044_finding_risk_score.up.sql
apps/frontend/components/layout/Sidebar.tsx
apps/frontend/utils/api-client.ts
docker-compose.yml
```

---

## Test Commands
```bash
cd apps/backend   && go test ./...
cd apps/goScanner && go test ./...
cd apps/frontend  && npx jest --no-coverage
```

---

## Session History
- Sessions 1–15: Core feature build (scan pipeline, findings, remediation, compliance)
- Session 16: Settings route split + full rebuild
- Session 17: Finding-evidence bug fix (MatchedValue field end-to-end)
- Session 18 (this): System audit P0+P1 — all shipped, PR #30 open
