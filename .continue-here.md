# Continue Here — ARC-HAWK-DD

**Updated:** 2026-04-06
**Branch:** cc

---

## What's Done (Uncommitted — all changes on branch `cc`)

### Session 1 Bug Fixes (previous session)
1. **PII Summary endpoint** — `GET /api/v1/scans/:id/pii-summary`
2. **Actions always visible** — Removed hover-only opacity from FindingsTable
3. **Confidence always 45%** — Boosted MLConfidence minimum in sdk_adapter.go
4. **Delete scan button** — `DELETE /api/v1/scans/:id` with cascading delete
5. **Dark theme remnants** — Fixed scans pages (bg-card → bg-white, etc.)

### Session 2 Fixes (previous session)
6. **Scanner `username` → `user` field mapping** — `scanner_api.py` normalizer
7. **Scanner diagnostic reporting** — Parses hawk_scanner stdout for tables/rows
8. **Backend diagnostic storage** — `CompleteScan` handler stores diagnostics
9. **Frontend diagnostic display** — Scan detail page shows status_message
10. **Delete connection route missing** — Added `DELETE /connections/:id`
11. **Delete source from Run Scan modal** — Trash icon on source cards
12. **FindingDetailDrawer dark theme fix** — Light theme colors

### Session 3 Fixes (this session) — Multi-Source Scanner Bugs
13. **BUG-1: MySQL Driver** — Added `_ "github.com/go-sql-driver/mysql"` import in `connection_test_service.go`
14. **BUG-2: MongoDB missing `args`** — Added `args` to `connect_mongodb()` call in `mongodb.py:106`
15. **BUG-3: MongoDB `username→user`** — Changed `config.get('username')` → `config.get('user')` in `mongodb.py:87`
16. **BUG-4: CouchDB normalizer + username** — Added `'couchdb','redis'` to DB_TYPES, changed `username→user` in `couchdb.py`
17. **BUG-5: MySQL port int** — `int(config.get('port', 3306))` in `mysql.py`
18. **BUG-6: MongoDB port int** — `int(config.get('port', 27017))` in `mongodb.py`
19. **BUG-7: Redis port int** — `int(config.get('port', 6379))` in `redis.py`
20. **BUG-8: CouchDB port int** — `int(config.get('port', 5984))` in `couchdb.py`
21. **BUG-9: PostgreSQL port int** — `int(config.get('port', 5432))` in `postgresql.py`
22. **BUG-10: Normalizer port coercion** — Added port→int coercion at boundary in `scanner_api.py`
23. **PyMongo bool fix** — `if db is not None:` instead of `if db:` in `mongodb.py:108`
24. **Scanner stderr logging** — Increased from 500 to full stderr, stripped ASCII banner

### Session 3 Fixes — PII Type Filtering & UI Bugs (partially done)
25. **PII type filter (PARTIALLY FIXED)** — Added filtering in `scanner_api.py` `ingest_results()` with short→internal name mapping (`PAN→IN_PAN`, `EMAIL→EMAIL_ADDRESS`, etc.). Scanner now filters findings by requested PII types. **Status: Code deployed, needs testing.**
26. **TotalFindings accumulation (DONE)** — Changed `ingest_sdk_verified.go:100` from `= acceptedFindingsCount` to `+= acceptedFindingsCount` so chunks accumulate instead of overwriting.
27. **RemediationConfirmationModal light theme (DONE)** — Rewrote `RemediationConfirmationModal.tsx`: replaced all dark theme classes (`text-white`, `bg-card`, `bg-secondary`, `bg-muted`, `text-amber-200`) with light theme (`text-slate-900`, `bg-white`, `bg-slate-50`, `text-amber-800`).
28. **Scan table whole-row click (DONE)** — Replaced per-cell `<Link>` wrappers with `onClick={() => router.push(...)}` on `<tr>` in `scans/page.tsx`. Delete button uses `e.stopPropagation()`.

---

## Session 4 Fixes — Dashboard, Assets, Findings Bugs

29. **Dashboard metrics logic (DONE)** — Removed refresh button and "Connecting..." notification from home page. Fixed `ActionsRequired` metric to count findings without review (pending) instead of using `enrichment_score`.
30. **Asset delete button (DONE)** — Added backend `DELETE /api/v1/assets/:id` with cascading delete (review_states → classifications → findings → asset). Added delete button in AssetTable. Frontend calls `assetsApi.deleteAsset()`.
31. **FindingDetailDrawer navbar overlap (DONE)** — Increased drawer z-index from z-50 to z-[70] and backdrop from z-40 to z-[60] to sit above the sticky navbar (z-50).
32. **"Back to Inventory" wrong URL (DONE)** — Changed `router.push('/asset-inventory')` to `router.push('/assets')` in `assets/[id]/page.tsx`.
33. **Findings search bar (DONE)** — Added `Search` field to `FindingFilters`, `FindingsQuery`, and `FindingsHandler`. Backend now searches across `asset name`, `asset path`, and `pattern_name` via ILIKE. Same filter added to `CountFindings`.
34. **AssetTable light theme (DONE)** — Fixed dark theme remnants: `bg-secondary`, `text-muted-foreground`, `bg-muted`, `divide-border`, `text-slate-200`, `bg-red-500/10` → light theme equivalents.
35. **FindingDetailDrawer bg-card fix (DONE)** — Changed header `bg-card` → `bg-white`.

---

## PREVIOUS REMAINING BUGS (from session 3 — verify these are actually fixed)

### Bug A: PII type filtering still scans all types
- **Root cause found & fixed**: Frontend sends short names (`PAN`, `AADHAAR`, `EMAIL`), scanner internal uses `IN_PAN`, `IN_AADHAAR`, `EMAIL_ADDRESS`. Added mapping dict in `scanner_api.py`.
- **Status**: Fix deployed to scanner container. **Needs test scan to verify.**
- **File**: `apps/scanner/scanner_api.py` — search for `_SHORT_TO_INTERNAL`

### Bug B: Findings column shows wrong count
- **Root cause found & fixed**: Each ingestion chunk overwrote `TotalFindings` instead of accumulating.
- **Status**: Fix deployed to backend. **Needs test scan to verify the count is correct.**
- **File**: `apps/backend/modules/scanning/service/ingest_sdk_verified.go:100`

### Bug C: Mask button modal has white text on white bg
- **Root cause found & fixed**: Full rewrite of `RemediationConfirmationModal.tsx` with light theme.
- **Status**: Frontend rebuilt. **Needs visual verification in browser.**
- **File**: `apps/frontend/components/remediation/RemediationConfirmationModal.tsx`

### Bug D: Scan table rows only clickable in specific spots
- **Root cause found & fixed**: Replaced per-cell `<Link>` with whole-row `onClick` + `useRouter`.
- **Status**: Frontend rebuilt. **Needs visual verification in browser.**
- **File**: `apps/frontend/app/scans/page.tsx`

---

## Modified Files (all sessions combined, uncommitted)

### Backend (Go)
- `apps/backend/modules/connections/service/connection_test_service.go` (MySQL driver import)
- `apps/backend/modules/connections/module.go` (DELETE route)
- `apps/backend/modules/scanning/api/scan_status_handler.go` (diagnostics storage)
- `apps/backend/modules/scanning/service/sdk_adapter.go` (confidence boost)
- `apps/backend/modules/scanning/service/ingest_sdk_verified.go` (TotalFindings accumulation)
- `apps/backend/modules/scanning/module.go` (new routes)
- `apps/backend/modules/shared/infrastructure/persistence/scan_run_repository.go` (DeleteScanRun, GetScanPIISummary)

### Scanner (Python)
- `apps/scanner/scanner_api.py` (normalizer, diagnostics, PII type filter with name mapping, logging)
- `apps/scanner/hawk_scanner/commands/mongodb.py` (args, user, port int, bool fix)
- `apps/scanner/hawk_scanner/commands/mysql.py` (port int)
- `apps/scanner/hawk_scanner/commands/redis.py` (port int)
- `apps/scanner/hawk_scanner/commands/couchdb.py` (port int, username→user)
- `apps/scanner/hawk_scanner/commands/postgresql.py` (port int)

### Frontend (React/Next.js)
- `apps/frontend/app/scans/page.tsx` (whole-row click, useRouter, light theme)
- `apps/frontend/app/scans/[id]/page.tsx` (PII summary, diagnostics, light theme)
- `apps/frontend/components/FindingsTable.tsx` (actions always visible)
- `apps/frontend/components/findings/FindingDetailDrawer.tsx` (light theme)
- `apps/frontend/components/remediation/RemediationConfirmationModal.tsx` (full light theme rewrite)
- `apps/frontend/components/scans/ScanConfigModal.tsx` (delete source icon)
- `apps/frontend/services/scans.api.ts` (deleteScan, getScanPIISummary)

---

## Test Infrastructure

Local test databases via `C:\Users\AnshGupta\Desktop\arc_hawk_infra`:
- **PostgreSQL:** host.docker.internal:5433, db=pii_test_db, user=postgres, pass=password123
- **MySQL:** host.docker.internal:3306, db=pii_test_db, user=archawk, pass=password123
- **MongoDB:** host.docker.internal:27017, db=archawk_vault, user=admin, pass=password123

## How to Resume

```bash
claude
# Then say:
# "Read .continue-here.md and resume — verify the 4 remaining bug fixes work"
```

### Verification steps:
1. Open browser → Scans page → verify whole-row click works (Bug D)
2. Click Mask on any finding → verify modal has dark text on white bg (Bug C)
3. Trigger a scan selecting only 2-3 PII types → verify only those types appear in findings (Bug A)
4. After scan completes, verify Findings column shows correct count (Bug B)
5. If all verified, commit all changes
