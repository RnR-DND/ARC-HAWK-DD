apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: celery-worker-ingest-agent-scaler
  namespace: hawk
  labels:
    app.kubernetes.io/name: arc-hawk-dd
    app.kubernetes.io/component: celery-worker-ingest-agent
spec:
  scaleTargetRef:
    name: celery-worker-ingest-agent
  pollingInterval: 15
  cooldownPeriod: 120
  minReplicaCount: 2
  maxReplicaCount: 20
  triggers:
    - type: redis
      metadata:
        address: redis.hawk.svc.cluster.local:6379
        listName: ingest_agent
        listLength: "50"
        activationListLength: "1"
