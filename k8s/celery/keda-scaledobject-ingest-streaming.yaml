apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: celery-worker-ingest-streaming-scaler
  namespace: hawk
  labels:
    app.kubernetes.io/name: arc-hawk-dd
    app.kubernetes.io/component: celery-worker-ingest-streaming
spec:
  scaleTargetRef:
    name: celery-worker-ingest-streaming
  pollingInterval: 15
  cooldownPeriod: 120
  minReplicaCount: 2
  maxReplicaCount: 20
  triggers:
    - type: redis
      metadata:
        address: redis.hawk.svc.cluster.local:6379
        listName: ingest_streaming
        listLength: "50"
        activationListLength: "1"
