apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: celery-worker-escalation-scaler
  namespace: hawk
  labels:
    app.kubernetes.io/name: arc-hawk-dd
    app.kubernetes.io/component: celery-worker-escalation
spec:
  scaleTargetRef:
    name: celery-worker-escalation
  pollingInterval: 15
  cooldownPeriod: 120
  minReplicaCount: 2
  maxReplicaCount: 20
  triggers:
    - type: redis
      metadata:
        address: redis.hawk.svc.cluster.local:6379
        listName: escalation
        listLength: "50"
        activationListLength: "1"
